name: Send Canva Design to LINE

on:
  schedule:
    # ★修正: 月〜金の日本時間12:00 (UTC 03:00) に実行
    # 1=Mon, 2=Tue, 3=Wed, 4=Thu, 5=Fri
    - cron: '0 3 * * 1-5'
  workflow_dispatch:

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.9'

      - name: Install dependencies
        run: pip install requests

      - name: Run script
        id: script_step
        env:
          CANVA_CLIENT_ID: ${{ secrets.CANVA_CLIENT_ID }}
          CANVA_CLIENT_SECRET: ${{ secrets.CANVA_CLIENT_SECRET }}
          CANVA_REFRESH_TOKEN: ${{ secrets.CANVA_REFRESH_TOKEN }}
          CANVA_DESIGN_ID: ${{ secrets.CANVA_DESIGN_ID }}
          LINE_CHANNEL_ACCESS_TOKEN: ${{ secrets.LINE_CHANNEL_ACCESS_TOKEN }}
          # LINE_USER_ID は全員送信(broadcast)になったので削除しました
        run: python main.py

      - name: Update Refresh Token Secret
        if: steps.script_step.outputs.new_refresh_token != ''
        env:
          GH_TOKEN: ${{ secrets.PA_TOKEN }}
        run: |
          gh secret set CANVA_REFRESH_TOKEN --body "${{ steps.script_step.outputs.new_refresh_token }}"
